<%= form_with(model: poisson) do |form| %>
  <% if poisson.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(poisson.errors.count, "error") %> prohibited this poisson from being saved:</h2>

      <ul>
        <% poisson.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :lamdapar, style: "display: block" %>
    <%= form.text_field :lamdapar %>
  </div>

  <div>
    <%= form.label :k, style: "display: block" %>
    <%= form.number_field :k %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
